# @streetstudio/types

Product-level shared type aliases used across the StreetStudio client packages
(`player`, `editor`, `timeline`, `ui`). No runtime code, no dependencies.

Framework/wire types (DTOs, error taxonomy) live in `@streetstudio/shared`;
this package holds UI/product primitives such as `Seconds`, `MarkerKind`, and
`EntityRef`.
