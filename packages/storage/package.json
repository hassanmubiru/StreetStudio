{
  "name": "@streetstudio/storage",
  "version": "0.1.0",
  "private": true,
  "description": "Storage abstraction: the StorageProvider contract, routing, and signed-target policy. Providers ship as plugins.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsc -b" },
  "dependencies": {
    "@streetstudio/shared": "*",
    "@streetstudio/auth": "*"
  },
  "streetstudio": { "domain": "Storage abstraction: the StorageProvider contract, routing, and signed-target policy. Providers ship as plugins." }
}
