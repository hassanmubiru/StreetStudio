# @streetstudio/auth

**Primary domain responsibility:** Authentication, sessions, RBAC, and API keys.

Public API is exposed exclusively through the package entry point.

## Dependencies

- `@streetstudio/database`
- StreetJS (`@streetjs/core`) — authentication/authorization primitives via published public entry points.
