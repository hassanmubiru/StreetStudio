{
  "name": "@streetstudio/playback",
  "version": "0.1.0",
  "private": true,
  "description": "Playback: authorized streaming of completed uploads (with HTTP range support).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsc -b" },
  "dependencies": {
    "@streetstudio/shared": "*",
    "@streetstudio/uploads": "*",
    "streetjs": "^1.2.7",
    "@streetjs/storage": "^1.0.2"
  },
  "streetstudio": { "domain": "Playback: authorized streaming of completed uploads over StreetJS." }
}
