# @streetstudio/ui

**Primary domain responsibility:** Shared UI components for web and desktop clients.

Public API is exposed exclusively through the package entry point.

## Dependencies

- `@streetstudio/shared`
