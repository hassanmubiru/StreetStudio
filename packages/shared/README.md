# @streetstudio/shared

**Primary domain responsibility:** Cross-cutting types, DTOs, errors, and constants shared across all packages.

Public API is exposed exclusively through the package entry point (`.` → `dist/index.js`). Importing internal modules directly is a boundary violation.

## Dependencies

None. This is the foundation package of the dependency graph.
