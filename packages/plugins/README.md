# @streetstudio/plugins

**Primary domain responsibility:** Plugin_Manager, plugin contracts, and isolation.

Public API is exposed exclusively through the package entry point.

## Dependencies

- `@streetstudio/shared`
- StreetJS (`@streetjs/core`) — plugin loading via published public entry points.
