# @streetstudio/config

**Primary domain responsibility:** Configuration schema and loading via StreetJS configuration.

Public API is exposed exclusively through the package entry point.

## Dependencies

- `@streetstudio/shared`
- StreetJS (`@streetjs/core`) — consumed only through its published public entry points.
