# @streetstudio/editor

The browser editor model: non-destructive `EditOperation`s (trim, split, merge,
crop, speed, captions, annotations) expressed over a `@streetstudio/timeline`
`Timeline`. Pure model types; rendering and UI live in the dashboard and
`@streetstudio/ui`.

## Dependencies

`@streetstudio/types`, `@streetstudio/timeline`.
