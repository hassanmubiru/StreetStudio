{
  "name": "@streetstudio/editor",
  "version": "0.1.0",
  "private": true,
  "description": "Browser editor model: trim, split, merge, crop, speed, captions, and annotations over a timeline.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsc -b" },
  "dependencies": {
    "@streetstudio/types": "*",
    "@streetstudio/timeline": "*"
  },
  "streetstudio": { "domain": "Browser editor model: trim, split, merge, crop, speed, captions, and annotations over a timeline." }
}
