# @streetstudio/sdk

**Primary domain responsibility:** Public client library for the StreetStudio REST and WebSocket API.

Public API is exposed exclusively through the package entry point.

## Dependencies

- `@streetstudio/shared`
