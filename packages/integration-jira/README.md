# @streetstudio/integration-jira

Jira integration delivered as an isolated plugin (Requirement 21.8). Implements
the `Plugin` contract from `@streetstudio/plugins`; discovered and loaded through
the StreetJS plugin loader. No Jira vendor SDK is imported into platform core.
