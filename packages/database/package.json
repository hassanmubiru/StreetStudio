{
  "name": "@streetstudio/database",
  "version": "0.1.0",
  "private": true,
  "description": "PostgreSQL schema, migrations, repositories, and the append-only audit log.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@streetstudio/shared": "*",
    "@streetstudio/config": "*"
  },
  "peerDependencies": {
    "@streetjs/core": "^0.1.0"
  },
  "peerDependenciesMeta": {
    "@streetjs/core": { "optional": true }
  },
  "streetstudio": {
    "domain": "PostgreSQL schema, migrations, repositories, and the append-only audit log.",
    "streetjs": ["@streetjs/core"]
  }
}
