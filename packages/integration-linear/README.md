# @streetstudio/integration-linear

Linear integration delivered as an isolated plugin (Requirement 21.8). Implements
the `Plugin` contract from `@streetstudio/plugins`; discovered and loaded through
the StreetJS plugin loader. No Linear vendor SDK is imported into platform core.
