{
  "name": "@streetstudio/uploads",
  "version": "0.1.0",
  "private": true,
  "description": "Uploads domain: chunked/resumable upload sessions that assemble real objects in storage.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsc -b" },
  "dependencies": {
    "@streetstudio/shared": "*",
    "streetjs": "^1.2.7",
    "@streetjs/storage": "^1.0.2"
  },
  "streetstudio": { "domain": "Uploads: chunked/resumable upload sessions assembling real objects in storage." }
}
