{
  "name": "@streetstudio/ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI capability router: routes AI requests to enabled provider plugins, or fails cleanly.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsc -b" },
  "dependencies": {
    "@streetstudio/shared": "*"
  },
  "streetstudio": { "domain": "AI capability router: routes AI requests to enabled provider plugins, or fails cleanly." }
}
