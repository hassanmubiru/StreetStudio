{
  "name": "@streetstudio/recordings",
  "version": "0.1.0",
  "private": true,
  "description": "Recordings domain: recording lifecycle (draft → published → archived), persistence, and API.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsc -b" },
  "dependencies": {
    "@streetstudio/shared": "*",
    "streetjs": "^1.2.7"
  },
  "streetstudio": { "domain": "Recordings domain: lifecycle, persistence, and API over StreetJS." }
}
