{
  "name": "@streetstudio/identity",
  "version": "0.1.0",
  "private": true,
  "description": "Identity: real member registration/login (Argon2id) + JWT issuance and shared auth helpers on StreetJS.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsc -b" },
  "dependencies": {
    "@streetstudio/shared": "*",
    "streetjs": "^1.2.7",
    "argon2": "^0.44.0"
  },
  "streetstudio": { "domain": "Identity: registration/login (Argon2id), JWT issuance, and shared auth helpers." }
}
