# @streetstudio/analytics

**Primary domain responsibility:** View events and aggregation.

Public API is exposed exclusively through the package entry point.

## Dependencies

- `@streetstudio/database`
