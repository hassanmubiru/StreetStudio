/**
 * @streetstudio/web
 *
 * Web_Client browser application entry point. Composes shared UI, the SDK, and
 * the recording client.
 */
export const DOMAIN = "Web_Client browser application." as const;
