# @streetstudio/recorder-extension

A browser extension that lets developers start a recording from the toolbar and
upload it through the public SDK — the fastest path to "New Explanation" without
opening the dashboard. Composes `@streetstudio/recorder` and `@streetstudio/sdk`;
talks to the API only through public interfaces.
