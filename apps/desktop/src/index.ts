/**
 * @streetstudio/desktop
 *
 * Desktop_Client entry point. Wraps the web client and adds native capture.
 */
export const DOMAIN =
  "Desktop_Client application wrapping the web client with native capture." as const;
